# 下一步执行方案

> 基于当前状态（Phase 0-2 完成，Phase 3 60%，Phase 4 未开始）
> 备份文件：`Phase 完成未测试UI修正备份.zip`

---

## 推荐执行顺序

```
Phase 3 收尾 → Phase 4 验证 → 清理 → 部署
```

---

## 一、Phase 3 收尾：修复 5 个审计遗留问题（预计 30 分钟）

### 1.1 `var(--n-color)` 死 CSS 变量
- **文件**: `src/views/GameStatus.vue`
- **操作**: 搜索所有 `--n-*` 变量，替换为 Arco 对应变量或移除
- **验证**: `grep -r "var(--n-" src/` 无输出

### 1.2 `.n-icon` 死选择器
- **文件**: `src/layout/DefaultLayout.vue`
- **操作**: 移除 `.n-icon` 相关 CSS 规则
- **验证**: `grep -r "\.n-icon" src/` 无输出

### 1.3 `handleThemeChange` 冗余
- **文件**: `src/App.vue`
- **操作**: 检查是否已内联到 useTheme.js，移除 App.vue 中的冗余实现
- **验证**: 主题切换功能正常

### 1.4 `addListener` 废弃 API
- **文件**: `src/composables/useTheme.js`
- **操作**: `mediaQueryList.addListener()` → `mediaQueryList.addEventListener('change', ...)`
- **验证**: 无浏览器控制台警告

### 1.5 `.n-form` 死选择器
- **文件**: `src/views/Login.vue`
- **操作**: 移除 `.n-form` 相关 CSS 覆盖
- **验证**: `grep -r "\.n-form" src/` 无输出

---

## 二、Phase 4 验证（预计 1-2 小时，取决于环境）

### 2.1 代码质量检查
```bash
cd /workspace/xyzw-project/04-dev
npm run lint
```
- 预期：0 error（warning 可接受）
- 如有 error，逐条修复

### 2.2 构建验证
```bash
npm run build
```
- **当前问题**: 沙箱内存不足导致 OOM killed
- **解决方案**:
  - **方案 A**: 在本地/更大内存环境中执行（推荐）
  - **方案 B**: 临时拆分 `BatchDailyTasks.vue` 为多个子组件，降低单文件内存占用
  - **方案 C**: 调整 Node 内存限制 `NODE_OPTIONS=--max-old-space-size=4096 npm run build`

### 2.3 运行时验证
```bash
npm run preview
```
- 检查所有页面是否正常渲染
- 检查 Token 管理 5 项功能（当前仅前 2 项正常）
- 检查主题切换
- 检查响应式布局

---

## 三、清理工作（预计 15 分钟）

### 3.1 移除 naive-ui 依赖
```bash
npm uninstall naive-ui
```
- 验证 `package.json` 中无 `naive-ui`
- 验证 `node_modules` 中无 `naive-ui`

### 3.2 移除死代码
- 检查 `src/` 中是否还有 `n-` 前缀的组件引用
- 检查是否还有未使用的 import

---

## 四、部署（预计 30 分钟）

### 4.1 GitHub 推送
```bash
git add .
git commit -m "Phase 3完成: UI框架迁移至Arco Design, 修复5个审计问题"
git push origin main
```

### 4.2 Cloudflare Pages 部署
- 连接 GitHub 仓库
- 构建命令: `npm run build`
- 输出目录: `dist`
- 环境变量: 按需配置 API 端点

---

## 五、风险与备选方案

| 风险 | 影响 | 备选方案 |
|------|------|----------|
| Build OOM 无法解决 | 无法部署 | 拆分 BatchDailyTasks.vue 为 3-4 个子组件 |
| Token 功能 3-5 项仍有 bug | 核心功能不可用 | 回退到 ZIP 备份，单独修复后再合并 |
| Arco Design 移动端适配差 | 用户体验差 | 引入 Arco Design Mobile 或 Vant 做移动端补充 |
| lint 大量 error | 延迟交付 | 分批次修复，先保证 build 通过 |

---

## 六、我的建议

1. **立即执行**: Phase 3 的 5 个修复（低风险，快速完成）
2. **优先解决**: Build OOM 问题 — 这是当前最大阻塞点
3. **暂缓决策**: 是否引入 Arco Design Mobile — 等构建通过后再评估移动端体验
4. **关键验证**: Token 管理 5 项功能 — 这是用户明确反馈有问题的核心功能

---

## 七、执行检查清单

- [ ] 5 个审计问题修复完成
- [ ] `npm run lint` 0 error
- [ ] `npm run build` 成功
- [ ] `npm run preview` 所有页面正常
- [ ] Token 管理 5 项功能全部正常
- [ ] naive-ui 已卸载
- [ ] GitHub 已推送
- [ ] Cloudflare Pages 已部署
