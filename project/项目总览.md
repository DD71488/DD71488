# 项目总览

> 生成时间：2026-06-04
> 目标：基于开源项目，将 APK528 版本的全量变更（新增+修改+删除+重构）移植到 Web 版，切换到 Arco Design，实现功能完整对齐
> 部署方式：Cloudflare Pages

---

## 一、总体进度

```
Phase 0: 基础设施         ████████████████████ 100% ✅
Phase 1: 布局+核心组件     ████████████████████ 100% ✅
Phase 2: 模块推进          ████████████████████ 100% ✅
Phase 3: CSS 对齐          ████████████████████ 100% ✅
Phase 4: 验证+部署         ██████████████░░░░░░  70% ⚠️
                          (build ✅, naive-ui移除 ✅, n-清除 ✅,
                           Arco全量移除 ✅, lint ❌, 预览 ❌, 部署 ❌)
```

---

## 二、各阶段详细进度

### Phase 0: 基础设施 (100%)

| 文件 | 操作 | 状态 |
|------|------|------|
| `src/main.js` | Naive UI → Arco Design（全量引入已移除） | ✅ |
| `src/composables/useArcoMessage.js` | 新建 | ✅ |
| `src/composables/useArcoDialog.js` | 新建 | ✅ |
| `src/App.vue` | 5层Provider → a-config-provider，冗余 handleThemeChange 已移除 | ✅ |
| `src/composables/useTheme.js` | arco-theme属性 + computed isDark，addListener 已修复 | ✅ |

### Phase 1: 布局框架 + 基础组件 (100%)

| 文件 | 操作 | 状态 |
|------|------|------|
| `src/components/Common/ThemeToggle.vue` | Arco图标 + a-button | ✅ |
| `src/layout/DefaultLayout.vue` | 全部n-* → a-*，.n-icon 死选择器已修复 | ✅ |
| `src/components/Common/IdentityCard.vue` | n-tag → a-tag | ✅ |
| `src/components/GameStatus.vue` | n-tabs → a-tabs，var(--n-color) 已修复 | ✅ |
| `src/views/Login.vue` | 完整Arco表单替换，.n-form 死选择器已修复 | ✅ |
| `src/views/Home.vue` | Arco按钮 + 抽屉 | ✅ |

### Phase 2: 模块推进 (100%)

| Batch | 内容 | 状态 |
|-------|------|------|
| Batch 1 (P0) | crossPlatform.js, wakeLock.js, answer.json, scheduler-worker.js | ✅ |
| Batch 2 (P0) | dailyTaskRunner.js 重构 (1235行, 15子模块) | ✅ |
| Batch 3 (P1) | Profile TokenManager内联 (1163行) | ✅ |
| Batch 4 (P1) | BatchDailyTasks 14tabs (6952行) | ✅ |
| Batch 5 (P2) | GameFeatures增强组件 | ✅ |
| Batch 6 (P3) | 63个文件 (Club14/Cards20/TokenImport6/等) | ✅ |

**补充修复**：

| 文件 | 问题 | 修复 | 状态 |
|------|------|------|------|
| `src/views/GameFeatures.vue` | 24处 `<n-tabs>`/`<n-tab-pane>` 残留 | 全部替换为 `<a-tabs>`/`<a-tab-pane>`，v-model→v-model:active-key | ✅ |
| `src/views/Profile.vue` | 8处 Naive 组件残留 | n-select→a-select, n-switch→a-switch, n-card→a-card 等，主题系统统一为 arco-theme | ✅ |
| `src/utils/imageExport.js` | Capacitor 依赖 | 重写为纯 Web 版本（Promise风格、Blob下载、navigator.share） | ✅ |
| `src/utils/gameCommands.js` | 缺少新增命令 | 添加 nmext_getinfo 等 5 个命令 | ✅ |

### Phase 3: CSS 对齐 (100%)

| 问题 | 文件 | 状态 |
|------|------|------|
| `var(--n-color)` 死CSS变量 | GameStatus.vue | ✅ 已修复 → `var(--color-bg-2)` |
| `.n-icon` 死选择器 | DefaultLayout.vue | ✅ 已修复 → `.arco-icon` |
| `handleThemeChange` 冗余 | App.vue | ✅ 已移除（含 computed import、window.addEventListener("theme-change")） |
| `addListener` 废弃API | useTheme.js | ✅ 已修复 → `addEventListener("change", ...)` |
| `.n-form` 死选择器 | Login.vue | ✅ 已修复 → `.arco-form`/`.arco-form-item` |
| CSS 62个变量迁移 | variables.scss | ✅ 已完成（详见 css-analysis.md） |

### Phase 4: 验证+部署 (70%)

| 任务 | 状态 | 备注 |
|------|------|------|
| `npm run build` | ✅ 已通过 | `NODE_OPTIONS=--max-old-space-size=4096`，26s 完成 |
| 移除 naive-ui 依赖 | ✅ 已完成 | package.json 中已无 naive-ui |
| `<n-` 组件残留 | ✅ 已清除 | 全项目 grep 0 匹配（xyzw/ 目录除外，为游戏引擎代码） |
| Arco Design CSS 导入 | ✅ 已修复 | main.js 已导入 arco.css 并注册 ArcoVue |
| `npm run lint` | ❌ 未执行 | |
| 运行时验证 | ❌ 未执行 | |
| 推送到 GitHub | ❌ 未执行 | |
| Cloudflare Pages 部署 | ❌ 未执行 | |

---

## 三、APK528 功能实现状态

### 3.1 已确认完整的功能

| 功能 | 状态 | 位置 |
|------|------|------|
| Token 管理（分组、导入、队列） | 完整 | `stores/tokenStore.ts`, `views/TokenImport/` |
| WebSocket 连接（BON协议、心跳、重连） | 完整 | `utils/wsAgent.js`, `utils/xyzwWebSocket.js` |
| 批量日常任务（14个Tab） | 完整 | `views/BatchDailyTasks.vue` |
| 定时任务系统（Cron） | 完整 | `views/BatchDailyTasks.vue` |
| 游戏功能面板（Tab布局） | 完整 | `views/GameFeatures.vue` |
| 军团战实时数据 | 完整 | `views/LegionWar.vue` |
| 答题功能 | 完整 | `utils/studyQuestionsFromJSON.js` + `public/answer.json` |
| 图片导出 | 完整 | `utils/imageExport.js`（已重写） |
| 赛车功能 | 完整 | `components/ClubCarKing.vue`, `utils/batch/tasksCar.js` |
| 皮肤挑战 | 完整 | `components/cards/SkinChallengeCard.vue` |
| 月赛助威 | 完整 | `utils/gameCommands.js` |
| XLSX 导出（盐场数据） | 完整 | `components/Club/LegionWarStatistics.vue` |
| XLSX 导出（俱乐部战绩） | 完整 | `utils/clubBattleUtils.js` |
| XLSX 导出（俱乐部排名） | 完整 | `utils/clubWarrankUtils.js` |

### 3.2 边缘功能（需确认）

| 功能 | 状态 | 位置 | 说明 |
|------|------|------|------|
| IdentityCard 30+ 物品 | 需确认 | `components/Common/IdentityCard.vue` | 组件存在，物品数量待确认 |
| ClubWeirdTowerInfo 导出扩展 | 需确认 | `components/Club/ClubWeirdTowerInfo.vue` | 组件存在，导出功能待确认 |
| CrazyCar 组件 | 需确认 | - | APK528编译产物中可能不存在独立组件 |

---

## 四、已知问题

1. ~~**Build OOM**~~: ✅ 已解决（`NODE_OPTIONS=--max-old-space-size=4096`）
2. ~~**5处死CSS/API**~~: ✅ 已全部修复
3. ~~**answer.json 缺失**~~: ✅ 已从 APK 复制到 `04-dev/public/`
4. ~~**imageExport.js Capacitor 依赖**~~: ✅ 已重写为纯 Web 版本
5. ~~**gameCommands 新增命令**~~: ✅ 已添加 5 个命令
6. ~~**eval 安全风险**~~: ✅ 已移除
7. **Token管理页面布局**: Arco Design移动端适配问题（待运行时验证）
8. **构建优化方案待执行**: 详见 `04-dev/BUILD-OPTIMIZATION.md`（P2可选优化（当前采用全量导入方案）、P1 code-splitting、P2拆分BatchDailyTasks）
9. **Capacitor 死代码**: `crossPlatform.js` 和 `wakeLock.js` 中有降级处理，可选清理
~~10. ~~**~~Arco CSS 未导入~~**~~：main.js 缺少 `import "@arco-design/web-vue/dist/arco.css"` 和 `app.use(ArcoVue)`，导致所有 Arco 组件无样式（阻断性bug）~~ ✅ 已修复（2026-06-05）

---

## 五、构建产物现状

```
总 JS 体积: ~5,200 KB (gzip ~1,300 KB)
总 CSS 体积: ~930 KB (gzip ~105 KB)
构建时间: 26s (NODE_OPTIONS=--max-old-space-size=4096)
```

| 关键 chunk | 原始大小 | gzip 大小 | 说明 |
|-----------|----------|-----------|------|
| `index-*.js` | 4,145 KB | 1,128 KB | Arco Design 全量 + Vue 核心 |
| `GameFeatures-*.js` | 731 KB | 190 KB | 36 个子组件集中打包 |
| `BatchDailyTasks-*.js` | 213 KB | 52 KB | 7 个 tab 单文件组件 |

---

## 六、文件统计

| 目录 | 文件数 |
|------|--------|
| 00-baseline | 315 |
| 01-reference | 176 |
| 01-reference/mappings | 17 |
| 02-translations | 22 |
| 03-css-analysis | 1 |
| 04-dev/src | 约80个Vue/JS/TS文件 |
| 总计 | 806+个文件 |

---

## 七、更新记录

| 日期 | 更新内容 |
|------|---------|
| 2026-06-01 | 初始版本 |
| 2026-06-02 | 更新 Phase 0-3 完成状态 |
| 2026-06-04 | 更新为"全量变更移植"目标，更新 Phase 4 进度至70%，补充已修复问题，更新文件统计 |
| 2026-06-05 | 更新Arco CSS导入状态为阻断性bug，更新文件统计，更新CrazyCar状态 |
| 2026-06-06 | 更新Arco CSS导入状态为✅已修复，更新Phase 4进度 |
