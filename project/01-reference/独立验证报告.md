# 独立验证报告 — 五项操作执行结果

> 执行时间：2026-06-06
> 目的：对已有参考文件进行独立验证，确认其准确性

---

## 一、apk-raw 与 formatted-js 抽样 diff 验证

### 方法

选取 5 个不同大小的文件，去除所有空白字符后逐字符对比。

### 结果

| 文件 | 差异比例 | 差异类型 |
|------|---------|---------|
| `imageExport-BfNAYNiL.js` | 1.77% | 尾随逗号、箭头函数括号 |
| `DailyTasks-ObamfrKF.js` | 3.10% | 尾随逗号、箭头函数括号 |
| `Profile-CnTTXBgA.js` | 2.63% | 尾随逗号、import 逗号 |
| `BatchDailyTasks-CDlX_GQ6.js` | 2.62% | 尾随逗号、参数括号 |
| `GameFeatures-Dixlrubn.js` | 2.48% | 尾随逗号、冗余括号移除 |

**所有差异均为 Prettier 格式化产物，无任何逻辑差异。** ✅

| 验证项 | 结果 |
|--------|------|
| 变量名是否一致 | ✅ 完全一致 |
| 函数名是否一致 | ✅ 完全一致 |
| 字符串内容是否一致 | ✅ 完全一致 |
| 数字常量是否一致 | ✅ 完全一致 |
| 导入路径是否一致 | ✅ 完全一致 |
| 控制流结构是否一致 | ✅ 完全一致 |

---

## 二、映射表混淆变量名验证

### 方法

从 5 个映射表中各抽取 5 个混淆变量名，在 formatted-js 中搜索验证。

### 结果

| 映射表 | 抽查数 | 准确 | 不准确 | 行号偏差 |
|--------|--------|------|--------|---------|
| BatchDailyTasks_mapping | 5 | 5 | 0 | 无 |
| GameFeatures_mapping | 5 | 5 | 0 | 1 条偏移 +38 行（已标注 ~） |
| Profile_mapping | 5 | 5 | 0 | 2 条偏移 +1 行 |
| imageExport_mapping | 5 | 5 | 0 | 无 |
| DailyTasks_mapping | 5 | 5 | 0 | 无（未提供行号） |
| **合计** | **25** | **25** | **0** | **—** |

**准确率：100%** ✅

行号偏差：3/20（有行号引用的条目）= 15%，最大偏差 38 行（已在映射表标注 `~` 近似值）。

### 额外发现

- `DailyTaskRunner_mapping` 文件不存在于 mappings 目录中，但 DailyTaskRunner 的映射关系在 `imageExport_mapping.md` 和 `BatchDailyTasks_mapping.md` 中均有覆盖
- `BatchDailyTasks-CDlX_GQ6.js` 已做别名还原（import 中直接使用可读名称），与其他文件不同

---

## 三、04-dev 关键文件与标注文档对比

### 3.1 crossPlatform.js

| 标注要求 | 实际状态 | 验证 |
|---------|---------|------|
| `CrossPlatformStorage` 类（6 个方法） | 完整实现（set/get/remove/clear/getEnvironment） | ✅ |
| `isTauri` / `isCapacitor` | 已实现 | ✅ |
| `storage` 单例 | 已导出 | ✅ |

### 3.2 dailyTaskRunner.js

| 标注要求 | 实际状态 | 验证 |
|---------|---------|------|
| `ERROR_CODE_MAP`（23 条） | 已实现（23 条） | ✅ |
| `CONTEXT_ERROR_MAP`（6 模块） | 已实现（6 模块） | ✅ |
| `extractErrorCode` / `getErrorMessage` / `isKnownError` / `isConnectionError` | 已实现 | ✅ |
| `build*Tasks` 方法（标注 11 个） | **实际 10 个**（buildBasicTasks/buildSaltBottleTasks/buildArenaTask/buildBossTasks/buildFixedRewardTasks/buildActivityTasks/buildBlackMarketTask/buildDreamWorldTask/buildDeepSeaLampTask/buildRewardTasks） | ⚠️ 少 1 个 |
| `ensureConnection` / `isConnected` | 已实现 | ✅ |
| `sendCommandSafe` | 已实现 | ✅ |
| `getCurrentFormation` / `switchFormation` / `restoreFormation` | 已实现 | ✅ |

### 3.3 BatchDailyTasks.vue

| 标注要求 | 实际状态 | 验证 |
|---------|---------|------|
| `saveCardStatus` / `restoreCardStatus` | 已实现（行 5584/5608） | ✅ |
| `isWeekResetNeeded` | 已实现（行 5554） | ✅ |
| `startStarChallenge` / `loadStarChallengeData` | 已实现（行 5790/5980） | ✅ |
| `fetchCarStatus` / `fetchArenaRank` / `startArenaFight` / `claimLegacy` | 已实现 | ✅ |
| `eval(taskName)` 安全风险 | **已移除（搜索无匹配）** | ✅ 已修复 |

### 3.4 Profile.vue

| 标注要求 | 实际状态 | 验证 |
|---------|---------|------|
| `TokenManager` 内联组件 | 已实现（使用 `useLocalTokenStore`） | ✅ |
| `maskToken` | 已实现（行 359） | ✅ |
| `getTokenMenuOptions` | 已实现（行 427） | ✅ |
| `handleMenuSelect`（需 5 个 break） | **已实现，包含全部 5 个 break 语句**（行 467-485） | ✅ 已修复 |
| `clearAllTokens` | 已实现（行 737） | ✅ |
| `refreshGameToken` | 已实现（行 523） | ✅ |

### 3.5 组件文件验证

| 组件 | 文件路径 | 状态 |
|------|---------|------|
| `CarScoreInfo` | `04-dev/src/components/Club/CarScoreInfo.vue` | ✅ 存在 |
| `DailyTaskStatus` | `04-dev/src/components/Daily/DailyTaskStatus.vue` | ✅ 存在 |
| `CrazyCar` | 搜索 `**/CrazyCar*` | ❌ **未找到** |

### 3.6 wakeLock.js

| 标注要求 | 实际状态 | 验证 |
|---------|---------|------|
| `WakeLockManager` | 文件存在 | ✅ |

---

## 四、gameCommands 缺失命令验证

### 方法

在 `gameCommands-BYMl-9HL.js` 中逐一搜索 32 个命令字符串和 13 个响应映射字符串。

### 命令搜索结果

| 状态 | 数量 | 详情 |
|------|------|------|
| ✅ 存在 | **3** | `activity_buystoregoods`、`store_getpurchase`、`discount_getdiscountinfo` |
| ❌ 缺失 | **29** | 其余 29 个命令均不存在 |

**原报告声称"约 20 个缺失"，实际为 29 个，报告低估了约 9 个（45%）。**

### 响应映射搜索结果

| 状态 | 数量 | 详情 |
|------|------|------|
| ✅ 存在 | **0** | — |
| ❌ 缺失 | **13** | 全部 13 条响应映射均不存在 |

**原报告声称"约 13 条缺失"，实际为 13 条，完全吻合。**

### 具体缺失清单

**缺失的 29 个命令：**
`activity_claimweekactreward`、`activity_claimredquenchreward`、`activity_claimrolluppack`、`activity_battlepassrewardclaim`、`charge_claimaddup`、`pet_activatebook`、`pet_claimbookreward`、`pet_openegg`、`nmext_getinfo`、`nmext_drawturntable`、`nmext_claimstarreward`、`nmext_startboss`、`nightmare_clickturntable`、`nightmare_claimweekreward`、`nightmare_claimturnrewardtimes`、`nightmare_claimbook`、`evotower_claimlegiontask`、`evotower_claimlegionprivilege`、`gacha_claimstagereward`、`tower_readyfight`、`presetteam_typegetinfo`、`typecalcpowerbyteam`、`typesetteam`、`warguess_getguessinfo`、`warguess_guessclaim`、`pkroom_appoint`、`hb_quench`、`hb_upgradeorder`、`legion_storegoodslist`

**缺失的 13 条响应映射：**
`charge_claimaddupresp`、`store_getpurchaseresp`、`evotower_claimlegiontaskresp`、`evotower_claimlegionprivilegeresp`、`activity_battlepassrewardclaimresp`、`pet_activatebookresp`、`pet_claimbookrewardresp`、`pet_openeggresp`、`warguess_getguessinforesp`、`warguess_guessclaimresp`、`legion_storebuygoodsresp`、`legion_storegoodslistresp`、`tower_readyfightresp`

---

## 五、CSS 分析报告选择器与实际文件对比

### 方法

从 24 个 CSS 文件中抽样 5 个，逐项验证报告中的选择器、变量数量、断点、动画。

### 5.1 选择器验证

| 文件 | 报告列出的选择器 | 实际存在 | 准确率 |
|------|----------------|---------|--------|
| BatchDailyTasks | 14 个关键选择器 | 全部存在 | 100% |
| GameFeatures | 大量选择器 | 全部存在 | 100% |
| DailyTasks | 12 个关键选择器 | 全部存在 | 100% |
| Profile | 12 个关键选择器 | 全部存在 | 100% |
| Login | 24 个关键选择器 | 全部存在 | 100% |

**选择器准确率：~100%** ✅

### 5.2 变量数量验证

| 文件 | 报告值 | 实测值 | 偏差 |
|------|--------|--------|------|
| BatchDailyTasks | 68 | **62** | **-6** |
| GameFeatures | 92 | **67** | **-25** |
| DailyTasks | 63 | **62** | **-1** |
| Profile | 62 | 62 | 0 |
| Login | 62 | 62 | 0 |

**GameFeatures 偏差最大（报告声称 92 实际 67，其中"新增约 30 个"实为 5 个）。**

### 5.3 关键声明验证

| 声明 | 报告值 | 实测值 | 结果 |
|------|--------|--------|------|
| `.n-*` 选择器数量 | ~28 个 | **28 个** | ✅ 完全准确 |
| 深色主题变量标准 | 62 个 | 62 个（大多数文件） | ✅ 主体准确 |
| MessageTester 无深色主题 | 无 | 无 | ✅ 准确 |
| WebSocketTester 无深色主题 | 无 | 无 | ✅ 准确 |
| 响应式断点 | 13 个 | 13 个 | ✅ 准确 |
| 关键帧动画 | 与报告一致 | 与报告一致 | ✅ 准确 |

---

## 六、综合结论

| 验证项 | 报告评估 | 独立验证结果 | 偏差 |
|--------|---------|-------------|------|
| apk-raw vs formatted-js | "逻辑完全一致" | ✅ 确认：100% 逻辑一致 | 无 |
| 映射表准确性 | "整体准确，行号有偏移" | ✅ 确认：100% 准确，3 条轻微偏移 | 无 |
| 04-dev 功能完整性 | "P1 16/20 已完成" | ⚠️ 大部分确认，但：CrazyCar **未找到**，build*Tasks 为 **10 个**（非 11 个） | CrazyCar 可能缺失 |
| 缺失命令数量 | "约 20 个" | ❌ 实际 **29 个**，报告低估 45% | **+9 个** |
| 缺失响应映射 | "约 13 条" | ✅ 确认：13 条 | 无 |
| CSS 变量数量 | 部分文件有误 | ❌ GameFeatures 高估 25 个，BatchDailyTasks 高估 6 个 | **显著偏差** |
| CSS 选择器 | "匹配度约 95%" | ✅ 确认：选择器描述准确 | 无 |
| `.n-*` 28 个 | 纠正正确 | ✅ 确认：28 个 | 无 |
| handleMenuSelect break | "需补充" | ✅ 04-dev 已修复（5 个 break 齐全） | 无 |
| eval(taskName) | "已修复" | ✅ 04-dev 中无 eval() | 无 |

### 需要修正的结论

1. **缺失命令数量**：从"约 20 个"修正为 **29 个**
2. **已存在的命令**：`activity_buystoregoods`、`store_getpurchase`、`discount_getdiscountinfo` 在 gameCommands 中实际存在，不应列入缺失清单
3. **CSS 变量数量**：GameFeatures 从 92 修正为 **67**，BatchDailyTasks 从 68 修正为 **62**
4. **CrazyCar 组件**：04-dev 中未找到，标注应为"缺失"而非"待确认"
5. **build*Tasks 方法**：实际为 10 个，非 11 个